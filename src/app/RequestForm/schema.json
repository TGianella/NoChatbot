{
  "type": "object",
  "properties": {
    "request": {
      "type": "string",
      "minLength": 3,
      "description": "Please enter your request"
    },
    "urls": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 3,
        "pattern": "^(https?|ftp)://[^\\s/$.?#].[^\\s]*$",
        "minItems": 1,
        "uniqueItems": true,
        "message": {
          "pattern": "test",
          "minLength": "coucou"
        }
      }
    }
  },
  "required": [
    "request",
    "urls"
  ]
}